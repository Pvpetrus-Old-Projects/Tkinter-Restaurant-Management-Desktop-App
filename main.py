from tkinter import *
from PIL import ImageTk, Image

# label, button, entry,
# grid, column, row, command (lambda do parametrów, text, bg, fg, pierwszy parametr oznacza gdzie coś przynalezy,
# pack pakuje, get,
# insert dodaje default text, columnspan, rowspan, height, padx, pady
# width, borderwidth, insert, delete, root.quit wychodzi z programu

root = Tk()
root.title("Restaurant Manager")
root.iconbitmap("Resources/restaurant_4373.ico")



root.mainloop()

